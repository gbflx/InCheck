import { StyleSheet, StatusBar } from "react-native";

const Styles = StyleSheet.create({
    container:{
       alignItems:'center' 
    },
    title:{
     display:'flex', 
    marginTop: 90,
    
    }
})

export default Styles;